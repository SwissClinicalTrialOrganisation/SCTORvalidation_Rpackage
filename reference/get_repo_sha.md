# get the most recent commit SHA for a repo

get the most recent commit SHA for a repo

## Usage

``` r
get_repo_sha(repo = sctotests(), branch = "main")
```

## Arguments

- repo:

  github repository to download from

- branch:

  branch to get the SHA from
