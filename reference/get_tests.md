# download test files from the validation_tests repo

download test files from the validation_tests repo

## Usage

``` r
get_tests(pkg, dir = tempdir(), repo = sctotests())
```

## Arguments

- pkg:

  package name as a string

- dir:

  directory to download to

- repo:

  github repository to download from
