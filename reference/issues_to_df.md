# issues are a list of items. this function converts it to a dataframe

issues are a list of items. this function converts it to a dataframe

## Usage

``` r
issues_to_df(issues, fun = extract_elements_pkg)
```

## Arguments

- fun:

  function to apply to each issue (extract_elements_pkg or
  extract_elements_test)
